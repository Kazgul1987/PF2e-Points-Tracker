/**
 * Stable ApplicationV2 facade.
 *
 * Tracker-specific context, dialogs, and transitional event adapters live in the controller module;
 * consumers retain the historical import path and exported class names.
 */
export { PointsTrackerApp, ResearchTrackerApp } from "./controllers/points-tracker-controller.js";
